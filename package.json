{
  "name": "web-form-login-management",
  "version": "1.0.0",
  "description": "Google Apps Script を TypeScript + clasp で管理する Web フォームログイン管理システム",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "push": "clasp push",
    "pull": "clasp pull",
    "start": "npm run build && clasp push"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "@types/google-apps-script": "^1.0.16",
    "@google/clasp": "^2.3.1"
  }
}
